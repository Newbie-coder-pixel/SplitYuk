{
  "name": "splityuk-relay",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Stateless notification relay for the SplitYuk app (PRD §13). Holds the Fonnte/email API keys server-side and forwards one notification request at a time — it never logs or stores bill, contact, or message content.",
  "scripts": {
    "dev": "vercel dev",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
